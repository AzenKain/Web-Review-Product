{
  "name": "web_review",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm --prefix packages/next-app run dev\" \"npm --prefix packages/nest-app run start:dev\"",
    "dev:next": "npm --prefix packages/next-app run dev",
    "build:next": "npm --prefix packages/next-app run build",
    "build": "npm --prefix packages/next-app run build && npm --prefix packages/nest-app run build"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
